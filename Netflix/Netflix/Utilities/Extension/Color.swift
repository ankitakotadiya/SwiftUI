import Foundation
import SwiftUI

extension Color {
    enum Custom {
        static let lightGray: Color = Color(.netflixLightGray)
        static let gray: Color = Color(.netflixGray)
        static let red: Color = Color(.netflixRed)
        static let darkRed: Color = Color(.netflixDarkRed)
    }
}
