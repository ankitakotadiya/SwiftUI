import Foundation
import SwiftUI

extension Color {
    enum Custom {
        static let background: Color = Color(.spotifyBackground)
        static let gray: Color = Color(.spotifyGray)
        static let green: Color = Color(.spotifyGreen)
        static let lightGray: Color = Color(.spotifyLightGray)
    }
}
